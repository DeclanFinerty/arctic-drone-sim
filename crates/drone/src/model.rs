//! Drone parameter structs and defaults are currently colocated with
//! `dynamics.rs` (see `PointMassQuad::arctic_small_quad`). This module is
//! reserved for named preset factories when a second drone class is added.
