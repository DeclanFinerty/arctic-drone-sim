//! Performance metrics computed over run histories are currently colocated
//! with each `Mission` implementation (see `StationKeep::score`). This module
//! is reserved for cross-mission summaries when a second mission is added.
